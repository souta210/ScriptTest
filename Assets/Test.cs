using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {


	// Use this for initialization
		
	void Start ()
	{
		int[] array = {5,8,14,56,79};
		for (int a=0;a<5;a++){
			Debug.Log (array[a]);
		}
			for (int b = 4; b > 0; b--) {
				Debug.Log (array [b]); 
			}

				
	}

	// Update is called once per frame
void Update ( ) {
		}
 }